{
  "name": "feyizino-events",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Static site. React 18 is vendored in vendor/ and loaded from a plain script tag - there is no bundler.",
  "scripts": {
    "dev": "node dev.js",
    "build": "node build.js",
    "preview": "node dev.js dist"
  }
}
